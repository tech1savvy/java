// available in java.lang package
// can neither be intiated because its constructor is private
	// and nor can be extended because it it declared as final

// has three important public static fields
	// `out` : standard output stream
	// `err` : standard error stream
	// `in` : standard inpit stream

// System.console() : returns java.io.Console object
public class Main {
  public static void main(String[] args) {
		long CTM = System.currentTimeMillis();
    System.out.println(CTM);
  }

	// System.arraycopy();
	
}
